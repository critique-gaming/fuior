enum character alice
enum character bob

enum animation_alice worried
enum animation_bob idle

var some_stat: number @min(-10) @max(10) = 0
var some_flag: boolean = false

declare_cmd log   "..." any
declare_cmd log_var varname varname
declare_cmd set  varname varname
declare_cmd unset  varname varname

declare_cmd animate  char character  animation string

declare_cmd wait  seconds number
